# Security Policy

## نطاق المشروع

MCP Conformance Lab أداة فحص واختبار قبل الدمج وليست sandbox أو firewall. تشغيل target عبر stdio معطل افتراضيًا، لكن explicit opt-in قد يمنح الخادم صلاحيات النظام المتاحة للعملية. اختبر الخوادم غير الموثوقة داخل container أو VM disposable.

## الإبلاغ عن ثغرة

لا تنشر تفاصيل استغلال غير مصحح في issue عامة. أرسل وصفًا خاصًا إلى maintainer عبر قناة أمنية موثوقة، متضمنًا الإصدار، البيئة، خطوات إعادة الإنتاج، والأثر المتوقع، مع إزالة أي secrets أو بيانات خاصة. سنحاول تأكيد الاستلام خلال سبعة أيام وتنسيق إصلاح وإفصاح مسؤول.

## مبادئ التعامل مع البيانات

لا يرسل المشروع محتوى الأدوات أو النتائج إلى خدمة خارجية. الأدلة محلية، وقد تحتوي على outputs يختار المستخدم اختبارها؛ تعامل معها كبيانات حساسة. headers تأتي من environment ولا تُكتب في evidence، لكن لا تحفظ bundle في مستودع عام إذا احتوت outputs سرية.
